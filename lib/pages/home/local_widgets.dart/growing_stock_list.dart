import 'package:flutter/material.dart';

class GrowingStockList extends StatelessWidget {
  const GrowingStockList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}